{
  "rules": {
    "prayers": {
      ".read": "auth != null && (auth.token.email == 'ted@narrative.church' || auth.token.email == 'matt@narrative.church')",
      ".write": "auth != null && (auth.token.email == 'ted@narrative.church' || auth.token.email == 'matt@narrative.church')"
    }
  }
}
